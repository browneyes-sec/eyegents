name: testing-patterns
version: 1.0.0
description: Test generation patterns and strategies
tools: [vector_search, filesystem, shell]
patterns:
  - name: unit_test
    description: Unit test pattern (AAA)
    example: |
      // describe("UserService", () => {
      //   it("should hash password on create", async () => {
      //     // Arrange
      //     const input = { email: "test@test.com", password: "secret" };
      //     // Act
      //     const result = await service.create(input);
      //     // Assert
      //     expect(result.passwordHash).not.toBe(input.password);
      //   });
      // });
  - name: integration_test
    description: Integration test with testcontainers
    example: |
      // it("should persist user to database", async () => {
      //   const container = await PostgreSqlContainer.start();
      //   const pool = new Pool({ connectionString: container.getConnectionUri() });
      //   await runMigrations(pool);
      //   // test...
      // });
  - name: e2e_test
    description: Playwright E2E test
    example: |
      // test("user can register", async ({ page }) => {
      //   await page.goto("/register");
      //   await page.fill("[name=email]", "test@test.com");
      //   await page.fill("[name=password]", "secret123");
      //   await page.click("button[type=submit]");
      //   await expect(page).toHaveURL("/dashboard");
      // });